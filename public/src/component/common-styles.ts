import * as React from 'react';
import { theme } from '../theme';
const cardStyles = {
  margin: theme.spacing(2)
};

const commonStyles = {
  cardStyles
};

const getNumberSignStyles = (n: number): React.CSSProperties => {
  if(n > 0) {
    return {

    }
  }
}



export { commonStyles };
